/// @description 여기에 설명 삽입
// 이 에디터에 코드를 작성할 수 있습니다
with (obj_player)
{
	other.x = x;
	other.y = y;
	other.is_head = is_head;
	other.image_xscale = dir;
}
if (is_head)
{
	//visible = true;
}
else
{
	//visible = false;
}
alarm[0] = 1;